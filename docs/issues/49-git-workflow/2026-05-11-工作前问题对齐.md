# sha.sh 工作流移植 — 工作前问题对齐

> 本文件基于 `2026-05-11-sha-sh-工作流移植评估-与-可配置-工作流引擎设计.md`（AI 分析报告）梳理的待决策问题。请逐项回复，我将按你的回复推进实现。

---

## 一、范围与边界

### 1.1 移植范围确认

设计报告建议第一阶段只覆盖 `dev` 和 `issue` 两个子命令（约 400 行 bash），暂不处理 `ws`/`submodule`/`sync`/`clean`/`test_colors`。

**Q01**: 这个范围是否需要调整？比如：
- 是否要把 `ws` 工作区批量执行也纳入第一阶段？
- `sync all` 的三个子步骤（submodule → xx → nodejs）是否需要在第一版一并统一入口？
- 是否有任何 `sha.sh` 中的命令你明确认为**不应移植**（比如只用于本地调试的临时命令）？

A: 只移植issue/dev git开发流

### 1.2 TypeScript 实现与 bash 脚本的共存关系

报告建议通过 `./sha.sh` vs `npx xx-cli workflow` 两个入口明确区分。

**Q02**: 移植完成后，旧的 `sha.sh` 如何处理？
- A) 保留并行运行，逐步迁移后删除
- B) 第一阶段完成后立即替换（`sha.sh` 改为对 xx-cli 的薄包装）
- C) 永远保留 bash 版本作为备份

A: 暂时保留，双轨运行，后面我手工删除

### 1.3 文件组织结构

报告建议：
```
packages/xx-cli/src/common/workflow/
├── index.ts    # createWorkflow() + re-export
├── types.ts    # 类型定义
├── config.ts   # 默认配置 + 合并
├── github.ts   # GhProvider 接口 + GhCliProvider
├── git.ts      # GitProvider 接口 + GitCliProvider
├── dev.ts      # DevWorkflow
└── issue.ts    # IssueWorkflow
```

而当前 `workflow.ts` 是个空文件。

A: 还未开始开发。

**Q03**: 
- 是改成 `workflow/` 目录结构（多个文件），还是保持单文件 `workflow.ts`？
- 如果改成目录，是否接受多文件拆分的复杂度？（类型文件、mock 文件还会更多）
- 是否需要单独一个 `workflow-cli.ts` 放 Commander 挂载（报告第 6 节），还是直接写在 `cli.ts` 里？

A: 多文件还是单文件无所谓，主要是aiagent方便.
workflow-cli.ts

---

## 二、配置设计

### 2.1 配置来源

报告建议通过 `WorkflowConfig` 接口配置，并提供默认值。但**配置从哪里读取**未明确。

**Q04**: 配置来源优先级？
- A) 仅代码传入（`createWorkflow({...})`），由 cli.ts 硬编码 xx 项目配置
- B) 从 `xx.yaml` / `xx.json` 读取（与现有 xx-cli 配置体系统一）
- C) 代码传入 + 环境变量覆盖（如 `GITHUB_REPOSITORY` 自动拆分 owner/repo）
- D) 三者都支持，优先级：命令行/代码 > 环境变量 > 配置文件

A: B从xx.yaml,暂时没那么复杂，先看效果

### 2.2 配置最小集

报告中 `gh.owner` / `gh.repo` / `gh.projectNum` / `gh.projectId` 被标记为必填。但 `sha.sh` 中实际只用到 4 个：

```bash
GH_OWNER="xx-agent"
GH_PROJECT_NUM="2"
GH_PROJECT_ID="PVT_kwDOENC_Tc4BXDLA"
GH_REPO="xx-agent/xx"
```

A: 理论上只用到2个, GH_OWNER,GH_PROJECT_NUM,
GH_PROJECT_ID可以从GH_PROJECT_NUM获取，GH_REPO可以'git'命令获取当前执行目录，类似'uv'，会把当前执行命令目录作为上下文。

**Q05**:
- `gh.repo` 和 `gh.owner` 是分开还是合并为一个字段？当前 `sha.sh` 有时用 `${GH_REPO}`（如 `xx-agent/xx`），有时单独用 `${GH_OWNER}`。报告的设计是分开的。
- `gh.projectId` 是否可以通过 `gh project list` 自动推导（避免用户查到 PVT ID 这种识别码）？


A: 可以合并,也可以分开，这个看设计简便性你评估吧。

### 2.3 分支解析策略

报告提供 `branchParser.parseIssueNumber` 作为注入点，默认实现兼容现有三种格式。`docs/dev-flow.md` 和 `docs/workflow-github.md` 均无其他格式的先例。

**Q06**: 在 xx 项目自身，分支名约定未来是否会改变？例如：
- 是否考虑引入 `feat/42-xxx` 或 `fix/42-xxx` 前缀？
- 是否可能一个分支对应多个 issue？（如 `42+43-foo`）

A: 分支名只是个惯用名称, 目前分支和问题1:1, 不然提交pr会搞出问题,另外可以用一个 xx.state.json或xx.lock.json之类的文件装载更多临时数据

---

## 三、Provider 抽象层

### 3.1 抽象层次

报告设计了 `GhProvider` 和 `GitProvider` 接口，默认用 `GhCliProvider` / `GitCliProvider` 封装 CLI。

**Q07**:
- 是否需要支持 **非 GitHub** 后端（如 GitLab）作为 `v1` 目标？如果是，接口设计需在 v1 就考虑多态。如果不是，v1 可以简化（不抽接口，直接实现）。
- `GitProvider` 是否必要？`git` 命令跨平台一致，且 mock 可以在集成测试层做（如创建临时 git repo），是否可以先不做 Provider 抽象，直接调用？

A: 不抽象，简化

### 3.2 错误处理策略

`sha.sh` 使用 `set -e`（遇错停止），`run()` 函数对失败命令透传退出码。

**Q08**:
- TypeScript 实现中，`gh` / `git` 命令失败时，是**抛异常**（停止整个工作流）还是**返回值 + 调用方决策**？
- `gh project item-list` 超 100 items 时，当前用 `--limit 100`。是否需要分页支持？

A: 遇错停止,打印cli命令，api调用

---

## 四、特定工作流行为的对齐

### 4.1 issue new 后自动加入 Project

当前 `sha.sh issue new` 的行为是 `gh issue create` → 提取编号 → `gh project item-add`。但 `gh issue create` 本身有 `--project` 参数（需要 project title）。

**Q09**: 是否改用 `gh issue create --project "xx"` 一步完成？还是保持两步（先创建再添加）以保证兼容性？

A: 可以，简化


### 4.2 issue dev 的 worktree 目录名

当前：
```bash
worktree_path="$_worktree_dir/$branch_name"
# 结果: .worktree/42-hot-reload
```

**Q10**: 如果 `.worktree/` 下已存在同名目录但**不是**当前仓库的 worktree（如残留），是继续报错退出还是有交互式选项（覆盖/跳过/退出）？

A: 报错退出，指导解决


### 4.3 dev pr 未提交变更的处理

当前代码会打 warning 但**不阻止**（`exit 1` 被注释掉）：

```bash
if [[ -n "$(git status --porcelain)" ]]; then
  echo "${error}warning: You have uncommitted changes. Please commit them before creating a PR.${reset}"
  # exit 1
fi
```

**Q11**: TypeScript 版本是否恢复 `exit 1`（严格禁止不提交发 PR），还是保持 warning 继续？

A: 报错退出，指导解决

### 4.4 dev merge_pr 是否允许 non-interactive

当前 `gh pr merge --squash` 是纯 squash，无交互确认。如果 PR 有冲突会直接失败。

**Q12**:
- 是否需要增加 `--confirm` 交互确认？（TypeScript 版本可以通过 `readline` 做）
- 是否容忍 merge 失败，还是需要在调用前检查 mergeable 状态？

A: 报错退出，指导解决，通常是web页面操作，这里只是为有些简单task项目提供一个简单入口，
可以默认交互模式，加参数非交互模式给ai使用，提前检查便于更好用户处理，报错的话打印异常错误就太长了，作为最后手段


### 4.5 dev remove 的安全性

当前逻辑：
1. 切回 main → pull
2. `git worktree remove` 
3. `git branch -d`（`-d` 是安全删除，未合并会报错）

**Q13**:
- 是否需要增加**预检**（检查是否有未推送的 commit / 是否有未合并的 PR）？
- 如果远程分支已合并但本地 `git branch -d` 报未合并，是否改用 `-D` 强删？

A:加预检，打印状态报告后，没问题执行，有问题退出指导
远程分支已合并但本地 `git branch -d` 报未合并，是因为未pull对吧，只用保证远程pr已合并没问题即可-D删除，打印git命令

### 4.6 缺失的 `issue ready` 命令

报告 3.2.6 指出 sha.sh 缺少 `Backlog → Ready` 过渡，当前靠 Web UI 手动拖拽。

**Q14**: 是否需要在第一阶段补充 `issue ready <num>` 命令，把 issue 从 Backlog 搬到 Ready？

A: new命令创建的issue直接进入ready状态，因为Backlog里可能有用户提交的需要预检的issue，而我们自己new的其实隐含已经预检过的

---

## 五、输出格式对齐

### 5.1 表格输出

`sha.sh` 用 bash 的颜色变量 + `column -t` 实现表格。TypeScript 版本输出怎么处理？

**Q15**:
- 是否沿用 `chalk`（`package.json` 已有依赖）做彩色输出？
- `dev list` 的表格格式（列宽/对齐/颜色）是否需要与 bash 版完全一致？
- `issue list` 列：`Issue | Status | Prio | Repository | Module | Title`。是否增减列（如 Assignee）？

A: packages/xx-tui 开发了tui框架，直接用

### 5.2 机器可读输出

`dev list` 在 bash 中已经内部构造了 JSON（虽然最终输出格式化表格）。

**Q16**: 是否需要支持 `--json` / `--format json` 输出原始数据（方便脚本调用）？

A: --json


---

## 六、测试策略

### 6.1 测试方式

报告建议通过 mock `GhProvider` / `GitProvider` 做单元测试。

**Q17**:
- 第一阶段的测试范围？
  - A) 仅类型检查（`tsc --noEmit`）通过即可
  - B) 核心逻辑单元测试（mock 外部调用）
  - C) 集成测试（需要 gh/git 真实环境）
- 该项目用 `vitest`，是否延续？

A: vitest,集成测试，
因为本身逻辑不复杂，主要是外部操作，所以e2e测试作为重点，我可以准备你需要的单独的测试project和repo，或给你一个test org，你在里面随便创建项目和repo进行测试

### 6.2 测试文件位置

项目现有测试文件平铺在 `packages/xx-cli/test/`：

**Q18**: workflow 的测试文件放在哪里？
- A) `packages/xx-cli/test/workflow/`（与现有风格一致）
- B) `packages/xx-cli/src/common/workflow/__tests__/`（与源码紧邻）

A: A


---

## 七、优先级与排序

### 7.1 实现优先级

按 AI 报告的模块拆分：

| 顺序 | 模块 | 内容 |
|------|------|------|
| 1 | types + config | 接口定义 + 默认配置 |
| 2 | github + git | Provider 实现 |
| 3 | issue.ts | `new` / `dev` / `list` |
| 4 | dev.ts | `list` / `status` / `pr` / `mergePr` / `remove` |
| 5 | index.ts + cli 挂载 | `createWorkflow()` + Commander 子命令 |

**Q19**: 这个顺序你认可吗？是否有需要先实现或后置的模块？

A: 没有Provider这种，就是github工作流，简化概念，不做过多抽象
顺序可以，看你了，


### 7.2 阶段性提交目标

**Q20**: 你是否希望按上述每个模块分别提交 PR（小步快跑），还是完成全部 5 个模块后一次性提交？

A: 小步提交，本身就在开发分支，随便折腾，咋简单咋来

---

## 八、补充

### 8.1 你是否有其他想法或约束？

请补充任何 AI 报告未覆盖但你认为重要的点，例如：
- 安全性要求（如 token 处理）
- 性能要求（如 `dev list` 预期最多处理多少个 worktree）
- 兼容性要求（Node.js 最低版本、操作系统支持）
- `docs/workflow-github.md` 中提到的「Agent 自动领取任务」流程是否需要在此次集成

A: node20+, os windows10+/linux/macos12+
暂时不自动领取，那是另一个任务了。

### 8.2 关于 AI 分析报告本身

**Q21**: AI 分析报告中有没有你觉得方向不对、需要推翻重来的部分？（如配置接口设计过于复杂、Provider 抽象过度等）

A: 太抽象，需要简化


---
chen56:另外
#### 3.2.2 🔴 Project 自定义字段名是硬编码的
A: 我决定减少对project的item的假设和干预，直接操作githubissue是不是可以减轻对字段的依赖？待评估，
list只列出当前workflow和issue的关系，并不对issue的字段做假设，比如完全不管project，只列issue的基本信息包括创建时间？


#### 3.2.3 🔴 worktree 目录名不可改
A: 固定 名称

#### 3.2.4 🔴 合并策略只有 squash
A: 和github对齐为可选择，github repo的合并选项是可配置的，api或gh拿到配置选项让用户选择,tui框架支持选择器
---

> 用户chen56已在每个问题用 "A: 回答"格式回答

---